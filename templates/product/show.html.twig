<h1>{{ product.name }}</h1>

<img src="{{ asset('uploads/images/' ~ product.image) }}" alt="{{ product.name }}" width="300">

<p>Prix : {{ product.price }} €</p>

{{ form_start(form) }}
    {{ form_row(form.size) }}
    <button type="submit">Ajouter au panier</button>
{{ form_end(form) }}

{% for message in app.flashes('success') %}
    <div class="flash-success">{{ message }}</div>
{% endfor %}
