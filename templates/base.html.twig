<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Stubborn{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('styles.css') }}">
      <link rel="icon" type="image/x-icon" href="{{ asset('uploads/images/favicon.png') }}">

   
    <style>
        a.active {
            font-weight: bold;
            text-decoration: underline;
        }
    </style>
</head>
<body>
<header class="main-header">
    <div class="brand">
        <img src="{{ asset('uploads/images/logo.png') }}" alt="Logo Stubborn">
        <div class="brand-text">
            <h1>Stubborn</h1>
            <small>Don't compromise on your look</small>
        </div>
    </div>

    <nav class="main-nav">
        {% set route = app.request.attributes.get('_route') %}
        {% if app.user %}
            <a href="{{ path('app_home') }}" class="{{ route == 'app_home' ? 'active' : '' }}">Accueil</a>
            <a href="{{ path('app_products') }}" class="{{ route == 'app_products' ? 'active' : '' }}">Boutique</a>
            <a href="{{ path('app_cart') }}" class="{{ route == 'app_cart' ? 'active' : '' }}">Panier</a>
            <a href="{{ path('app_logout') }}">Se déconnecter</a>
        {% else %}
            <a href="{{ path('app_home') }}" class="{{ route == 'app_home' ? 'active' : '' }}">Accueil</a>
            <a href="{{ path('app_register') }}" class="{{ route == 'app_register' ? 'active' : '' }}">S’inscrire</a>
            <a href="{{ path('app_login') }}" class="{{ route == 'app_login' ? 'active' : '' }}">Se connecter</a>
        {% endif %}
    </nav>
</header>


    <main>
        {% block body %}{% endblock %}
    </main>

    <footer class="main-footer">
    <div class="footer-content">
        <h3>À propos de Stubborn</h3>
        <p>
            Stubborn est une marque engagée qui conçoit des sweat-shirts uniques, éthiques et durables.
            Inspirés par la créativité et la détermination, nos produits sont pensés pour ceux qui osent affirmer leur style avec conviction.
        </p>

        <div class="footer-contact">
            <strong>Stubborn</strong><br>
            Piccadilly Circus, London W1J 0DA, Royaume Uni<br>
            <a href="mailto:stubborn@blabla.com">stubborn@blabla.com</a><br>
            <em>Don't compromise on your look.</em>
        </div>
    </div>
</footer>
</body>
</html>
