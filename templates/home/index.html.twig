{% extends 'base.html.twig' %}

{% block title %}Accueil - Stubborn{% endblock %}

{% block body %}
    <h1>Bienvenue chez Stubborn</h1>

    <section>
        <h2>Nos modèles phares</h2>

        <div class="featured-products">
            {% for product in products %}
                <div class="product-card">
                    <img src="{{ asset('uploads/images/' ~ product.image) }}" alt="{{ product.name }}" width="200">
                    <h3>{{ product.name }}</h3>
                    <p>{{ product.price }} €</p>
                    <a href="{{ path('app_product_show', {id: product.id}) }}">Voir le produit</a>
                </div>
            {% else %}
                <p>Aucun produit mis en avant pour le moment.</p>
            {% endfor %}
        </div>
    </section>

    <section>
        <h2>À propos de Stubborn</h2>
        <p>Stubborn est une marque engagée qui conçoit des sweat-shirts uniques, éthiques et durables. Inspirés par la créativité et la détermination, nos produits sont pensés pour ceux qui osent affirmer leur style avec conviction.</p>
    </section>
{% endblock %}
